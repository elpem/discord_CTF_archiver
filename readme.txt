pré-configuration :
    dans le .env, spécifier le nom de la guilde et le token

lancer par commandes : 
    pip3 install discord
    pip3 install python-dotenv

    python3 bot.py

lancer par Docker :
    (dans le dossier du projet)
    docker build . -t archiver
    docker run archiver

